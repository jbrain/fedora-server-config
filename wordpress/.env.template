# Copy to /opt/wordpress/.env and fill in before first run. Never commit the real .env.
# Must equal WORDPRESS_DB_PASSWORD in ../database/.env (the shared MariaDB container's
# least-privilege `wordpress` DB user password) — this container does NOT create that user,
# Task 01 already did.
WORDPRESS_DB_PASSWORD=changeme
