# Never commit real credentials
.env
*.env
wordpress/secrets/jba-ampache-api-key

# PHP build/test artifacts (regenerable via `composer install`)
vendor/
.phpunit.result.cache

# JS build artifacts (regenerable via `npm install && npm run build` in JS/cuber/)
JS/cuber/node_modules/
JS/cuber/dist/

# Editor / OS noise
.DS_Store
Thumbs.db
*.swp
*.swo
